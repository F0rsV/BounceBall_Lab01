public class Main {

    public static void main(String[] args) {
        TaskStarter.BallApp();

        //TaskStarter.SymbolsFirstTask();
        //TaskStarter.SymbolsSecondTask();

        //TaskStarter.CounterNoSync(new Counter());
        //TaskStarter.CounterNoSync(new CounterSyncLock());
        //TaskStarter.CounterNoSync(new CounterSyncMethod());
        //TaskStarter.CounterNoSync(new CounterSyncBlock());
    }
}
